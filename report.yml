---
- name: Server Report
  hosts: all
  become: yes
  gather_facts: true
  tasks:
    - name: Report Server Facts
      ansible.builtin.debug:
        msg:
          - "{{ ansible_hostname }}"
          - "{{ansible_distribution_release }}"